// Cálculo Simples

var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');

var linha1 = parseFloat(lines.shift()).split(" ");
var linha2 = parseFloat(lines.shift()).split(" ");
